{
  "name": "seo-dashboard-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w @seo-dashboard/client\" \"npm run dev -w @seo-dashboard/server\"",
    "build": "npm run build -w @seo-dashboard/shared && npm run build -w @seo-dashboard/client && npm run build -w @seo-dashboard/server",
    "lint": "npm run lint -w @seo-dashboard/client && npm run lint -w @seo-dashboard/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}